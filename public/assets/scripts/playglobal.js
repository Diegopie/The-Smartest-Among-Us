$(() => {
  // handle global button
  // $("#global").on("click", (event) => {
  //   event.preventDefault();
  //   $.ajax("/playglobal", {
  //     type: "GET",
  //   });
  // });

  $("#random").on("click", (event) => {
    event.preventDefault();
  });
});
